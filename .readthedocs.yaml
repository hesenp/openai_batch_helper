version: 2

build:
  os: ubuntu-latest
  tools:
    python: "3.11"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
    - requirements: doc/requirements.txt

sphinx:
  configuration: doc/conf.py

formats: []
